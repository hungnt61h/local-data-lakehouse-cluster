apiVersion: batch/v1
kind: Job
metadata:
  name: {{ starrocks_minio_bucket_job_name }}
  namespace: {{ starrocks_namespace }}
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: minio-client
        image: {{ starrocks_minio_bucket_job_image | quote }}
        imagePullPolicy: IfNotPresent
        command:
        - /bin/sh
        - -ec
        - |
          mc alias set minio {{ starrocks_minio_endpoint_internal | quote }} {{ starrocks_minio_access_key | quote }} {{ starrocks_minio_secret_key | quote }}
          mc mb --ignore-existing minio/{{ starrocks_minio_bucket }}
